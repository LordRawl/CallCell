include _externalIncludes
include _internalIncludes
include _mixins

-
  var projectName = 'CallCell'
  var site = "callcell.ru"
  var pageOgTitle = 'Аутсорс Колл-Центр 24/7'
  var pageOgDescription = 'Полностью ведем ваш проект, обучим операторов, заберем на себя задачи'

doctype html
html(lang='ru')
  include head

  body
    .wrapper
      +header
      
      .page
        block content
    
      +footer
      
    block scripts
    
    script(defer src='assets/scripts/app.min.js')
    script(async src='assets/scripts/animation.min.js')